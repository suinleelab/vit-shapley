# REQUIRED: update all three checkpoint paths to your trained models
classifier_ckpt: $checkpoint_dir/classifier/best_classifier.pth
attn_surrogate_ckpt: $checkpoint_dir/surrogate_attn/best_surrogate.pth
zero_surrogate_ckpt: $checkpoint_dir/surrogate_zero/best_surrogate.pth
model_name: vit_base_patch16_224
data_root: $data_dir
num_images: 50
num_masks: 50
step: 10
output: figures/surrogate_kl.png
device: ""
